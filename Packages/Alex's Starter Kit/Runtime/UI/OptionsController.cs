using System;
using System.Collections.Generic;
using UnityEngine;

namespace ASK.UI
{
    public class OptionsController : MonoBehaviour
    {
        /*private static Dictionary<Type, ToggleReceiver> _toggleReceivers = new();

        private void OnEnable()
        {
            _toggleReceivers.Clear();
            var receivers = GetComponents<ToggleReceiver>();
            foreach (var r in receivers)
            {
                _toggleReceivers.Add(r.GetType(), r);
            }
        }
        
        public static ToggleReceiver GetToggleReciever<T>() where T : ToggleReceiver
        {
            return _toggleReceivers[typeof(T)];
        }*/
    }
}